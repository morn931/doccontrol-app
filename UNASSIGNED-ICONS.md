# Unassigned canonical icons — CoreDocs

## Installed folders — all currently wired, none unused
`actions`, `administration`, `dashboard`, `developer-tools`,
`document-search`, `documents`, `mddr`, `reports`, `team`, `vendors` — every
folder copied into this repo is referenced by at least one file.

## Not yet installed here (belong to other modules, expected)
The rest of the 95-function pack — listed here only so a platform-wide
verification pass doesn't mistake "not in CoreDocs" for "missing".
